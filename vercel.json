{
    "version": 2,
    "builds": [
      { "src": "package.json", "use": "@vercel/static-build", "config": { "maxLambdaSize": "10mb" } }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "package.json" }
    ],
    "scripts": {
      "start": "gatsby develop -p $PORT"
    }
  }
  